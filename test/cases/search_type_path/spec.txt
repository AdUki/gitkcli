# Cycle to [Filepaths] (Tab x3: Txt -> ID -> Message -> Filepaths). Path search
# runs `git log -- *<query>*`; every commit touching a matching file is flagged.
# HEAD touches src/main.py, so 'n' steps to the first commit below HEAD that
# touches a *utils* file: 07cd75b "Clarify error path" [3/303].
size      120x40
launch
key       /
key       <Tab>*3
text      "utils"
key       <Enter>
wait      stable
key       n
wait      stable
capture   path_match
